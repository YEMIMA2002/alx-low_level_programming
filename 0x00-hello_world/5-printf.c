#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 always
 */

int main(void)
{
	char Grammar[60] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", Grammar);

	return (0);

}
